---
file-type: finding
status: verified
last-verified: 2026-04-25
supports-decisions: []
cites-sources:
  - ../README.md
  - ../decisions/README.md
  - ../themes/ai-paradox-invisible-use-cases/README.md
  - ../themes/ai-paradox-invisible-use-cases/live-thread.md
  - ../themes/ai-paradox-invisible-use-cases/vocabulary.md
  - ../themes/ai-paradox-invisible-use-cases/sources/deep-dives/tribe-v2-canonical-reference.md
  - ../themes/ai-paradox-invisible-use-cases/window-2-research-deepening/500-elicitation-qa-pass.md
  - ../themes/ai-paradox-invisible-use-cases/window-2-research-deepening/501-party-mode-roundtable.md
  - ../patterns/witnessed-dissent.md
  - ../projects/renaissance-research.md
cross-links:
  - prd-builder-reading-order-verification.md
---

# Lint pass — 2026-04-25

Karpathy lint operation against `research/wiki/` after consolidation lands at `ceae29c` + navigator skill at `743e26a`.

## Top-of-file health summary

| Check | Count | Severity |
|---|---|---|
| Contradictions held as named tensions | 2 (T1, T2) | ✅ correctly held |
| Contradictions silently averaged | 0 | ✅ |
| Stale claims to update | 0 (TRIBE v2 facts: deep-dive correctly supersedes source 006 with explicit `[contradiction noted; primary source wins]` markers) | ✅ |
| Orphan pages (no inbound links) | 0 across non-window-2 wiki files | ✅ |
| Missing concept pages | N/A — `concepts/` directory not yet created (deferred to post-consolidation per `300-wiki-redesign-blueprint.md`); vocabulary.md serves the lookup function for now | 🟡 expected gap |
| Broken cross-references | 1 (known stub — `_2026-sponsorship-footprint.md`, self-flagged in theme README as "discovery agent failed mid-run with an Anthropic overload error and needs a re-dispatch") | 🟡 self-flagged |
| Data gaps (claims without sources) | 0 detected on spot-check of decisions/ + new patterns/ + deep-dives | ✅ |

**Overall wiki health: 🟢 PRD-buildable.** No blocking issues. The single known broken link is a self-flagged stub the team can choose to re-run or accept as a known-unknown.

## Contradictions (held, not resolved)

### T1 — "Augmented human intelligence" framing IS a trends-slop default
- **Claim A** [source: `themes/.../sources/003-trends-slop-and-the-comment-section-in-flesh.md`:12]: Harvard 30K-data-point study found every model defaults to four corpus-mean answers regardless of context — *"differentiation, collaboration, long-term thinking, and **augmentation**."* Coined "trends slop."
- **Claim B** [source: `themes/.../README.md` + Johnny verbatim 2026-04-25]: hypothesis-frame adopted as **"Augmented human intelligence."**
- **How wiki holds it:** ✅ surfaced explicitly as held tension in:
  - `decisions/005-best-use-of-ai-as-hard-target.md`:58 — *"Picking 'augmented human intelligence' as the hypothesis framing means picking the trends-slop word as the un-trends-slop framing. This contradiction is **held, not resolved**."*
  - `vocabulary.md`:77 — augmentation entry lists *"trends slop; corpus mean"* as opposing terms.
  - `vocabulary.md`:199 — generic "Open tension (T-N)" entry catalogues T1–T10.
  - `projects/renaissance-research.md`:114 — surfaced as open question against the lookalike.
  - `decisions/004-socratic-protocol-installed.md`:44 — protocol explicitly instructs holding T1–T10.
  - `live-thread.md`:80 — protocol for logging tensions in changelog.
- **Status:** held correctly. PRD-builder will see it.

### T2 — Actor / Auditor / Mediator triad has no external referent
- **Claim A** [source: `themes/.../window-2-research-deepening/sources/004-multi-agent-alignment-actor-auditor-mediator.md`]: triad architecture is the un-black-boxing mechanism.
- **Claim B** [source: `themes/.../window-2-research-deepening/501-party-mode-roundtable.md` §8 cross-persona exchange]: without an external, non-platform-derived referent, the triad is structurally Filter World with extra steps.
- **How wiki holds it:** ✅ surfaced explicitly in:
  - `patterns/witnessed-dissent.md`:85, 93 — both anti-theme alignment + "no external referent = Filter World with extra steps" warning. Cites Mina Lee + Aydın personas.
  - `decisions/006-tribe-v2-as-special-mode.md`:63 — TRIBE v2 IS the candidate external referent the triad needs.
  - `decisions/007-agent-swarms-as-coordination-pattern.md`:63 — A7 question carried forward.
  - `themes/.../sources/deep-dives/tribe-v2-canonical-reference.md`:408 — TRIBE v2 explicitly claimed as "auditor's empirical instrument" — the only non-LLM external referent we have.
- **Status:** held correctly + a candidate resolution path is named (TRIBE v2 as the external referent). Whether to operationalize it is Johnny's call.

### TRIBE v2 source 006 vs deep-dive contradictions (resolved by primary-source authority)

Three load-bearing factual contradictions, all CORRECTLY held:

| # | Claim per source 006 | Claim per deep-dive | How wiki holds it |
|---|---|---|---|
| TR-1 | 70,000 voxels output | ~20,000 cortical-surface vertices on `fsaverage5` | `deep-dive:111` explicit `[contradiction noted; primary source wins]` |
| TR-2 | Trained on 700 subjects | Trained on ~25 deeply-scanned subjects (720 = eval universe) | `deep-dive:126` explicit `⚠️ Reconcile the numbers` block |
| TR-3 | (license not addressed in 006) | CC BY-NC-4.0; no commercial pathway; live license-violation issues on repo | `deep-dive:283` "Meta's published license — CC BY-NC 4.0 (verbatim core terms)" |

Deep-dive is correctly tagged as authoritative. PRD-builder pulling capability claims should cite the deep-dive paths, not source 006 paths.

## Stale claims

None requiring update. The TRIBE v2 corrections are already surfaced via the contradiction-held mechanism (above), not silently overwritten. Source 006 stays in the wiki as the verbatim transcript of what the YouTuber said; the deep-dive corrects the load-bearing facts.

## Orphan pages

None detected on spot-check of non-window-2 files. Every project / pattern / decision has at least one inbound reference from `wiki/README.md`, `decisions/`, theme README, or vocabulary. Window-2 research-deepening files are scratch/feedstock by design — they're indexed via the consolidation report, not via individual cross-links.

## Missing concept pages

The protocol's planned `concepts/<term>.md` directory does not yet exist. Per `300-wiki-redesign-blueprint.md`, the planned expansion would add ~12 concept pages (trends-slop, comprehension-debt, jagged-frontier, filter-world, augmented-intelligence, emotivist-layer, actor-auditor-mediator, mind-as-gap, double-distortion, witnessed-dissent, grounded-citation, un-black-box). For now, `vocabulary.md` provides the lookup function. **Decision deferred** — adding `concepts/` is an explicit user-authorization step (the blueprint flagged it as deferred until the consolidation lands; consolidation chose not to expand into it).

## Broken cross-references

1. `themes/ai-paradox-invisible-use-cases/README.md` → `../../../sponsors/_2026-sponsorship-footprint.md` — **🟡 self-flagged stub**. The theme README itself notes: *"(not yet populated; the discovery agent failed mid-run with an Anthropic overload error and needs a re-dispatch)."* The team can either re-run the sponsor-footprint agent or remove the link. Not blocking.

No other broken markdown links detected on spot-check of high-traffic files: `wiki/README.md`, `decisions/README.md`, `themes/.../README.md`, `live-thread.md`, `vocabulary.md`, `caltech/CONSOLIDATION-REPORT.md`, `projects/renaissance-research.md`, `projects/memento.md`, `patterns/witnessed-dissent.md`, `patterns/grounded-citation.md`. All 16 decision file links resolve. All 11 PRD-builder reading-order file links resolve.

(Earlier link-check noise was a `realpath -m` flag incompatibility on macOS; the actual file system is consistent.)

## Data gaps

None detected on spot-check. Decisions cite their trace pointers. Patterns cite their projects. Sources cite their URLs. Deep-dives mark `[verbatim]` on every primary-source quote.

## Suggested next questions / sources (for the team to choose; not ranked)

The wiki has well-marked gaps that aren't blockers but would strengthen the next phase:

1. **Re-dispatch the sponsor-footprint discovery agent** — produce `research/sponsors/_2026-sponsorship-footprint.md` so the theme README's broken link resolves and the team has the canonical map of which sponsors went to which past hackathons.
2. **Resolve T1 by either renaming the framing or owning the irony** — Johnny's call. PRD-builder needs the resolution.
3. **Operationalize TRIBE v2 as the auditor's external referent** (T2 candidate resolution) — concretely, where in the architecture does the auditor consult TRIBE v2 to ratify or reject an actor claim? Without operationalization the candidate stays aspirational.
4. **Verify the Harvard "trends slop" 30K-data-point study** (currently `[claimed; unverified by me]` in source 003) — find the actual paper, cite the authors, replace YouTuber-secondhand with primary citation.
5. **Verify the Krafton/Subnautica Delaware ruling primary source** — cite the actual case filings.
6. **Add a `concepts/` directory** if the wiki query-load increases — vocabulary.md is sufficient now but concept-page expansion is the natural next step per blueprint 300.
7. **Add a cross-window note for Window-1's ideation-sweep INDEX caveats** — when the PRD-builder reads `caltech/ideation/`, it must know which sections to drop per the INDEX (consolidation already enforces this for itself, but the navigator skill could call it out more loudly).
8. **Consider promoting `themes/ai-paradox-invisible-use-cases/sources/deep-dives/` to its own `entities/products/` folder** if more deep-dives accumulate — the file is structurally an entity page, not a source page, and the redesign blueprint anticipated this.

## Lint outcome

**Wiki health: 🟢 strong. PRD-buildable from this state.**

The Karpathy gates (dense / indexed / cross-referenced / provenance-honest / decision-traceable / scannable / small files / status-marked) are all met. The Socratic protocol is held — no file makes a project recommendation; tensions are surfaced not resolved; questions are surfaced not answered. The reading order resolves cleanly (verified separately in `prd-builder-reading-order-verification.md`).

**Single biggest blocker for PRD-buildability:** none structural. Two open tensions (T1, T2) await Johnny's hypothesis decisions but they're explicitly surfaced and the wiki is honest about not resolving them. The PRD-builder's first prompt should be to surface these to Johnny and ask for the framing call.

The wiki is ready to drive the PRFAQ in a separate Claude instance.
