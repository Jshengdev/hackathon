"use client";

// SLIDE 07 · SWARM DETAIL — 7 networks, 7 specialists, in parallel
// Speaker note: NOTES["swarm-detail"] · Persona-aware: NO
// Content lives in: persona-content/SwarmDetailContent.tsx

import type { Slide } from "../../components/Deck";
import SwarmDetailContent from "../persona-content/SwarmDetailContent";

const slide: Slide = {
  id: "swarm-detail",
  render: () => <SwarmDetailContent />,
};

export default slide;
