"use client";

// SLIDE 11 · FALSIFICATION — within-subject contrast · cosine doesn't lie
// Speaker note: NOTES.falsification · Persona-aware: NO
// Content lives in: persona-content/FalsificationContent.tsx

import type { Slide } from "../../components/Deck";
import FalsificationContent from "../persona-content/FalsificationContent";

const slide: Slide = {
  id: "falsification",
  render: () => <FalsificationContent />,
};

export default slide;
